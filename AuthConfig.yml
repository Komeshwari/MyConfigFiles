spring:
  boot:
    admin:
      client:
        url: http://localhost:1111/
  datasource:
    username: root
    password: manager
    url: jdbc:mysql://localhost:3306/ecommerceapp 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true           
management:
  endpoints:
    web:
      exposure:
        include: '*'
        
